{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
    "compilerOptions": {
      "jsx": "react-jsx", // Or "react-jsx" if you're using React 17+ and the new JSX transform
      "jsxImportSource": "react", // Esto es importante para que funcione la nueva transformación.
      "allowJs": true, // You already have this
      "esModuleInterop": true, // Usually necessary for imports to work correctly
      "lib": ["dom", "esnext"] // Make sure "dom" is included
  }
}